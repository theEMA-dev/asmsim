# Simple test program
addi $t0, $zero, 10   # Load 10 into $t0
addi $t1, $zero, 20   # Load 20 into $t1
add $t2, $t0, $t1     # Add $t0 and $t1, store in $t2
sub $t3, $t1, $t0     # Subtract $t0 from $t1, store in $t3